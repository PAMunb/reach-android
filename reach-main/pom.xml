<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>br.unb.cic.reach</groupId>
    <artifactId>reach-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>main</artifactId>

  <name>Reach :: Main Application</name>

  <properties>
    <main.class>br.unb.cic.reach.main.Main</main.class>
    <final.jar.name>reach-analyzer</final.jar.name>
  </properties>

  <dependencies>
    <dependency>
      <groupId>br.unb.cic.reach</groupId>
      <artifactId>core</artifactId>
    </dependency>
    <dependency>
      <groupId>br.unb.cic.reach</groupId>
      <artifactId>android</artifactId>
    </dependency>
    <dependency>
      <groupId>br.unb.cic.reach</groupId>
      <artifactId>jar</artifactId>
    </dependency>
    <dependency>
      <groupId>br.unb.cic.reach</groupId>
      <artifactId>mop</artifactId>
    </dependency>
    <dependency>
      <groupId>com.beust</groupId>
      <artifactId>jcommander</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <mainClass>${main.class}</mainClass>
            </manifest>
          </archive>
          <descriptorRefs>
            <descriptorRef>jar-with-dependencies</descriptorRef>
          </descriptorRefs>
          <finalName>${final.jar.name}</finalName>
          <appendAssemblyId>false</appendAssemblyId>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>